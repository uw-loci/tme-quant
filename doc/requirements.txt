# Reference dependencies (pyproject.toml is the source of truth).
# For installation, use: uv sync --extra curvelops
# Or: pip install -e ".[curvelops]"

# Core (from pyproject.toml)
pandas
numpy
matplotlib
scipy
opencv-python
scikit-image
openpyxl
napari
pyqt6
qtpy

# Optional curvelet backend
# curvelops @ git+https://github.com/PyLops/curvelops@0.23.4
